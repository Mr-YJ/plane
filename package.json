{
  "name": "plane",
  "version": "1.0.0",
  "description": "plane",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "packager": "electron-packager . electron-plane --overwrite --win=x32 --out  ./App --arch=x64 --app-version=1.0.0 --electron-version=2.0.0"
  },
  "author": "YJ",
  "license": "ISC",
  "devDependencies": {
    "electron": "^18.3.7",
    "electron-packager": "^13.1.1",
    "electron-winstaller": "^3.0.4"
  },
  "dependencies": {
    "electron-squirrel-startup": "^1.0.0"
  }
}
